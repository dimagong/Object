function Bird(name) {
  this.name = name;
  this.numLegs = 2;
}

let canary = new Bird("Tweety");
let ownProps = [];
// Add your code below this line

for( let kay in canary) {
  if(canary.hasOwnProperty(kay)) {
    ownProps.push(kay);
  }
}

console.log(ownProps);
